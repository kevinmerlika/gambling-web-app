export const pages = {
    login: '/login',       // Redirect to /login if the user is not authenticated
    error: '/error',       // Redirect to an error page in case of session fetching failure
    success: '/success',   // Redirect to success page after certain actions
    dashboard: '/Dashboard', // Add your dashboard URL here
    settings: '/Settings',  // Settings page URL
    // You can add more routes here as needed
  };
  